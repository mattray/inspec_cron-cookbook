{
    "reporter" : {
        "automate" : {
            "environment" : "<%= @environment %>",
            "insecure" : <%= @insecure %>,
            "node_name" : "<%= @node_name %>",
            "node_uuid": "<%= @node_uuid %>",
            "stdout" : false,
            "token" : "<%= @token %>",
            "url" : "<%= @url %>"
        }
    }
}
